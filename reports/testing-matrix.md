# Security Testing Matrix (Part D)

All 8 tests were executed via `bash security-tests/run_all_tests.sh` on
2026-07-11. Full terminal output for each run is in `evidence/` (filenames
below are the latest run of each test; older runs are also kept as evidence
of repeatability). The brief requires a minimum of 6 — this environment
ships 8.

| # | Test | Objective | Procedure | Expected Result | Actual Result | Evidence |
|---|------|-----------|-----------|------------------|----------------|----------|
| 1 | Unauthorized access | Confirm requests with no/invalid credentials are rejected | Configure `mc` with a bogus access key/secret and attempt `ls`; separately, anonymous `curl` GET on a private object | 403/AccessDenied, non-zero exit | **PASS.** `mc` alias creation failed ("Access Key Id... does not exist"), subsequent `ls` failed (exit 1). Anonymous `curl` returned HTTP 403 with `<Error><Code>AccessDenied</Code>`. | `evidence/test01_unauthorized_access_20260711T140449Z.log` |
| 2 | RBAC enforcement | Confirm each role can only act within its assigned bucket/permissions | hr-manager attempts to list finance-documents; finance-analyst (read-only) attempts to `PUT`; guest-contractor (valid login, no policy) attempts to list any bucket. Positive controls: each user reads their own bucket. | Positive controls succeed; all boundary-crossing attempts denied | **PASS.** hr-manager and finance-analyst read their own buckets successfully. hr-manager → finance-documents: `Access Denied` (exit 1). finance-analyst write attempt: `Insufficient permissions` (exit 1). guest-contractor: `Access Denied` (exit 1). | `evidence/test02_rbac_enforcement_20260711T140451Z.log` |
| 3 | TLS enforcement | Confirm data-in-transit is encrypted via the OpenSSL certificate | `openssl s_client` handshake + cert chain verification against `certs/ca.crt`; plaintext HTTP request against the same port | TLS handshake verifies against our CA; plaintext request does not return a valid 200 | **PASS.** Handshake: TLSv1.3, cipher `TLS_AES_128_GCM_SHA256`, `Verify return code: 0 (ok)`, cert `CN=minio` issued by `CN=DSA4030-Group4-RootCA`. Plaintext `curl http://...` returned HTTP 400, not the expected 200 health response. | `evidence/test03_tls_enforcement_20260711T140459Z.log` |
| 4 | Encryption at rest | Confirm objects are encrypted at rest (SSE-S3/AES256) | `mc encrypt info` on bucket; `mc stat` on object; raw byte inspection of the object's on-disk part file inside the MinIO container vs. the plaintext source file | AES256/SSE-S3 metadata present; on-disk bytes not readable JSON | **PASS.** `mc encrypt info` → `Auto encryption 'sse-s3' is enabled`. `mc stat` → `Encryption: SSE-S3`. Plaintext source began `{"document_type": "hr_record"...`; on-disk `part.1` bytes were unreadable binary ciphertext (`od -c` dump). | `evidence/test04_encryption_at_rest_20260711T140459Z.log` |
| 5 | Integrity verification | Confirm tampering with stored objects is detectable | Baseline SHA-256 vs. pre-upload manifest; directly corrupt bytes of the object's backend `part.1` file with `dd`; re-download; restore from the original source shard | Baseline matches; tampering causes read failure or hash mismatch; object is restorable | **PASS.** Baseline SHA-256 `88ac0ef9...` matched manifest exactly. After tampering, `mc cat` failed: `Resource requested is unreadable, please reduce your request rate` (MinIO's internal bitrot/checksum protection rejected the corrupted read). Object was re-uploaded from the original plaintext shard and re-verified hash-for-hash against the manifest. | `evidence/test05_integrity_verification_20260711T140501Z.log` |
| 6 | Audit log review | Confirm allowed and denied actions are both logged and queryable | Generate one authorized event (hr-manager reads own bucket) and one denied event (finance-analyst reads hr-documents); query the audit-webhook `/events` API by user | Both event types present with accessKey/API/status fields | **PASS.** Query for `user=hr-manager` returned 20 matched events (`GetObject`/`HeadObject`/`GetBucketLocation`, `statusCode: 200`). Query for `user=finance-analyst` returned 20 matched events (`GetBucketLocation`/`HeadBucket`, `statusCode: 403`). Raw `audit.log` confirmed append-only persistence. | `evidence/test06_audit_log_review_20260711T140508Z.log` |
| 7 | Malware scanning | Confirm ClamAV detects malicious content and clears legitimate documents | `clamdscan` against the EICAR industry-standard test file, a clean sample document, and a real dataset shard | EICAR flagged infected; clean files pass | **PASS.** EICAR file → `Eicar-Signature FOUND`, exit code 1. Clean sample → `OK`, exit code 0. Dataset shard spot-check → clean (no infection reported). | `evidence/test07_malware_scanning_20260711T140514Z.log` |
| 8 | Backup & recovery | Confirm a deleted object can be recovered without data loss | Mirror `contracts-documents` → `backups` bucket; delete an object (versioning creates a delete marker); confirm unreadable; restore via (a) previous object version and (b) the mirrored backup; re-verify hash | Object unreadable after delete, fully restored (hash matches baseline) after recovery | **PASS.** Delete created a delete marker; post-delete read failed (exit 1). Restored via `mc cp --version-id` (previous version) and independently via the mirrored `backups` bucket. Final SHA-256 matched the pre-deletion baseline. | `evidence/test08_backup_recovery_20260711T140514Z.log` |

**Result: 8/8 tests passed.** No control failed to enforce its intended
boundary during testing.
